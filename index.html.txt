<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Climate Factor | Decarbonize Your Future</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js"></script>
    <style>
        :root {
            --primary: #2af598; /* Mint Green */
            --secondary: #009efd; /* Tech Blue */
            --bg: #030a0d;
            --glass: rgba(255, 255, 255, 0.05);
            --glass-border: rgba(255, 255, 255, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
        }

        body {
            background-color: var(--bg);
            color: white;
            overflow-x: hidden;
            background: radial-gradient(circle at top right, #0a201a, #030a0d);
        }

        /* 3D Animated Background Element */
        .orb {
            position: fixed;
            width: 500px;
            height: 500px;
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            filter: blur(80px);
            border-radius: 50%;
            z-index: -1;
            opacity: 0.2;
            animation: float 15s infinite alternate;
        }

        @keyframes float {
            0% { transform: translate(-10%, -10%) rotate(0deg); }
            100% { transform: translate(50%, 20%) rotate(360deg); }
        }

        nav {
            padding: 2rem 10%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            backdrop-filter: blur(10px);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 800;
            letter-spacing: -1px;
            background: linear-gradient(to right, #fff, var(--primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero {
            height: 90vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 0 10%;
        }

        h1 {
            font-size: 5rem;
            line-height: 1.1;
            margin-bottom: 1.5rem;
            perspective: 1000px;
        }

        h1 span {
            display: block;
            background: linear-gradient(135deg, #fff 0%, #888 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            transform: translateZ(50px);
        }

        .hero p {
            font-size: 1.2rem;
            color: #a0a0a0;
            max-width: 600px;
            margin-bottom: 2.5rem;
        }

        .btn {
            padding: 1rem 2.5rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            transition: 0.3s;
            border: none;
            cursor: pointer;
        }

        .btn-primary {
            background: var(--primary);
            color: #030a0d;
            box-shadow: 0 10px 20px rgba(42, 245, 152, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 15px 30px rgba(42, 245, 152, 0.5);
        }

        /* 3D Glass Cards */
        .services {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            padding: 5rem 10%;
        }

        .card {
            background: var(--glass);
            border: 1px solid var(--glass-border);
            padding: 3rem 2rem;
            border-radius: 24px;
            backdrop-filter: blur(15px);
            transition: 0.5s;
            transform-style: preserve-3d;
        }

        .card:hover {
            transform: translateY(-20px) rotateX(10deg) rotateY(10deg);
            border-color: var(--primary);
            background: rgba(255, 255, 255, 0.08);
        }

        .card i {
            font-size: 2.5rem;
            color: var(--primary);
            margin-bottom: 1.5rem;
        }

        .card h3 {
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }

        .card p {
            color: #ccc;
            line-height: 1.6;
        }

        footer {
            padding: 4rem 10%;
            border-top: 1px solid var(--glass-border);
            text-align: center;
            color: #666;
            font-size: 0.9rem;
        }

        /* Mobile Adjustments */
        @media (max-width: 768px) {
            h1 { font-size: 3rem; }
        }
    </style>
</head>
<body>

    <div class="orb"></div>

    <nav>
        <div class="logo">CLIMATE FACTOR</div>
        <a href="mailto:consult.climatefactor@gmail.com" class="btn btn-primary" style="padding: 0.6rem 1.5rem; font-size: 0.9rem;">Contact Us</a>
    </nav>

    <section class="hero">
        <h1><span>Decarbonize</span> <span>Your Future.</span></h1>
        <p>Expert ESG reporting, Precision Carbon Accounting, and LCA strategies for companies that value the planet and the bottom line.</p>
        <div class="cta-group">
            <a href="#services" class="btn btn-primary">Our Solutions</a>
        </div>
    </section>

    <section class="services" id="services">
        <div class="card">
            <i class="fa-solid fa-chart-pie"></i>
            <h3>Carbon Accounting</h3>
            <p>Rigorous Scope 1, 2, and 3 measurement following global GHG protocols to ensure audit-ready data.</p>
        </div>
        <div class="card">
            <i class="fa-solid fa-leaf"></i>
            <h3>LCA Services</h3>
            <p>Complete Life Cycle Assessment of your products, identifying environmental hotspots from cradle to grave.</p>
        </div>
        <div class="card">
            <i class="fa-solid fa-shield-halved"></i>
            <h3>ESG Strategy</h3>
            <p>Building disclosure frameworks (GRI, SASB, TCFD) that satisfy investors and regulatory requirements.</p>
        </div>
    </section>

    <footer>
        <p>&copy; 2026 Climate Factor Consulting. All Rights Reserved.</p>
        <p style="margin-top: 10px;">Driven by AI. Rooted in Science.</p>
    </footer>

</body>
</html>